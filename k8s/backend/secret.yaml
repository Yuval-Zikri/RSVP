apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: rsvp-app
type: Opaque
stringData:
  # DATABASE_URL is now auto-generated from the HA Cluster

  # mail credentials
  MAIL_USERNAME: "noreply.eventnotify@gmail.com"
  MAIL_PASSWORD: "ausd jeve grzr gxmd"
